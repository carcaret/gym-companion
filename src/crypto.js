// crypto.js — vacío tras eliminar el sistema de autenticación.
// Las funciones sha256, xorEncrypt y xorDecrypt se han eliminado porque
// ya no se usan: el login se eliminó y el PAT se guarda en claro.
